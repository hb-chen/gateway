syntax = "proto3";
package com.hbchen;
option go_package = "proto";

service ExampleService {
    rpc Call (Request) returns (Response) {
    }
}

message Request {
    string name = 1;
}

message Response {
    string msg = 1;
}
