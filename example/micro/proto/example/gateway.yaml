type: google.api.Service
config_version: 3

http:
  rules:
    - selector: go.micro.srv.example.Example.Call
      post: /v1/example/call
      body: "*"
    - selector: go.micro.srv.example.Example.Call
      get: /v1/example/call/{name}
